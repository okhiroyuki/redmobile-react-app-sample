import React from 'react';
import ReactDOM from 'react-dom';
import Bar from './Bar'
import App from './App'

ReactDOM.render(
  <React.StrictMode>
    <Bar />
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
